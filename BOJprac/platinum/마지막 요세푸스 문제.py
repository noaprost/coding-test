# 1179
# import sys

# n, k = map(int, sys.stdin.readline().split())

# que = [i for i in range(1, n + 1)]

# front = 0

# while len(que) != 1:
#     front += k - 1
#     front = front % len(que)
#     que.remove(que[front])

# print(que[0])

# 메모리초과
